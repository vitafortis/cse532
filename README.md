# cse532
# If you don't have a virtual environment, do the following:
python3 -m pip install virtualenv
./venv/scripts/activate (works on windows, mac, linux)
python3 -m pip install -r requirements.txt
cd cse532
flask --app app run 
in a browser, navigate to localhost:5000

login: admin/admin (user/pass)
